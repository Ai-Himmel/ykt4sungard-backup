定义
#pragma pack(1)
typedef struct
{
	double price1; // 水价1
	double price2; // 水价2
	double price3; // 水价3
	double balance; // 余额
}WATER_PACK_INFO2;
#pragma pack()

// 写主钱包与水控钱包
/*
 \param main_money - 主钱包余额
 \param info - 水控钱包内容
 */
int __stdcall SMT_TransWaterPacket(int main_money,WATER_PACK_INFO2* info);

// 读取水控钱包
int __stdcall SMT_ReadWaterPackInfo(WATER_PACK_INFO2* info);
/// 写水控钱包
int __stdcall SMT_WriteWaterPackInfo(WATER_PACK_INFO2* info);