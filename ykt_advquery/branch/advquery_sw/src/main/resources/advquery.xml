  <Context path="${context.name}" docBase="E:/test/ecard/web" debug="99" reloadable="true">
    <Logger className="org.apache.catalina.logger.FileLogger" 
        prefix="log_${context.name}." suffix=".txt" timestamp="true"/>

    <Resource name="jdbc/${poolName}" auth="Container" type="javax.sql.DataSource"/>
    <ResourceParams name="jdbc/${poolName}">
			<parameter>
		     <name>factory</name>
		     <value>org.apache.commons.dbcp.BasicDataSourceFactory</value>
		   </parameter>
        <parameter>
            <name>maxActive</name>
            <value>${maximum.connection}</value>
        </parameter>
        <parameter>
            <name>maxIdle</name>
            <value>${maxidle.connection}</value>
        </parameter>
        <!-- Maximum time to wait for a dB connection to become available
             in ms, in this example 10 seconds. An Exception is thrown if
             this timeout is exceeded.  Set to -1 to wait indefinitely.
             -->
        <parameter>
            <name>maxWait</name>
            <value>10000</value>
        </parameter>
        <!-- DB2 dB username and password for dB connections  -->
        <parameter>
            <name>username</name>
            <value>${db.username}</value>
        </parameter>
        <parameter>
            <name>password</name>
            <value>${db.password}</value>
        </parameter>
        <!-- Class name for JDBC driver -->
        <parameter>
            <name>driverClassName</name>
            <value>${driver.class}</value>
        </parameter>
        
        <parameter>
            <name>dialect</name>
            <value>${dialect.classname}</value>
        </parameter>        

        <parameter>
            <name>url</name>
	    <value>${driver.url}</value>
        </parameter>
        
         <parameter>
              <name>removeAbandoned</name>
              <value>true</value>
         </parameter>

        <parameter>
            <name>removeAbandonedTimeout</name>
            <value>60</value>
        </parameter>
        <!-- Log a stack trace of the code which abandoned the dB 
             connection resources. 
             -->
        <parameter>
            <name>logAbandoned</name>
            <value>true</value>
        </parameter>
    </ResourceParams>
</Context>     
