create table t_cfgsplit
(
  transtype integer,
  transname varchar(60),
  dbflag char(1),
  crflag char(1),
  dbsubno varchar(20),
  crsubno varchar(20),
  dbaccno varchar(30),
  craccno varchar(30),
  fundtype integer,
  summary varchar(60)
)
begin
action modename ykt_cur end
action add end
action read_by transtype end
action update_by transtype end
action del_by transtype end
action read_lock_by c0 transtype end
action update_lock_by c0 end
action free_lock_by c0 end
end
