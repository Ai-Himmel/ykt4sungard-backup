create table t_devmonitor
(
  deviceid number,
  optime varchar2(30),
  coltime varchar2(30),
  devtime varchar2(30),
  beatstatus number,
  cardverno varchar2(12),
  hardiskper number,
  memoryper number,
  cpuper number
)
begin
action modename ykt_cur end
action add end
action read_by deviceid end
action update_by deviceid end
action del_by deviceid end
action read_lock_by c0 deviceid end
action update_lock_by c0 end
action free_lock_by c0 end
end
