create table t_geteleccard
(
  cardno integer,
  cardphyid varchar2(16),
  usetype integer,
  adddelflag number(1),
  deviceid integer,
  downtime varchar2(30)
)
begin
action modename ykt_cur end
action add end
action read_by cardno deviceid end
action del_by cardno deviceid end
action update_by cardno deviceid end
action read_lock_by c0 cardno deviceid end
action update_lock_by c0 end
action free_lock_by c0 end
end
