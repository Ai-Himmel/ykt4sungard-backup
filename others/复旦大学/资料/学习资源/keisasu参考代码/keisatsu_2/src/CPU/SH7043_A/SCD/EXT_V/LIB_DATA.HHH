#include "\src\atlanta\scd\define\lib_def.h"

/* 標準入出力ライブラリの初期化に必要なデータ外部宣言 */
struct _iobuf{
	UBYTE *_bufptr;	 	/* バッファへのポインタ */
	DWORD _bufcnt; 		/* バッファカウンタ */
	UBYTE *_bufbase; 	/* バッファベースポインタ */
	DWORD _buflen; 		/* バッファ長 */
	UBYTE _ioflag1; 	/* I/O フラグ */
	UBYTE _ioflag2; 	/* I/O フラグ */
	UBYTE _iofd; 		/* I/O フラグ */
};

extern struct _iobuf _job[_NFILE];
extern BYTE flmod[FLMAX] = {0};			/* オープンしたファイルのモード設定場所 */
