会议表状态标志
status(0正常，1修改，9删除)

与会人员列表YKT_CONF.T_ATTENDEE_LIST
send_sign(0未发送，1下发名单)
del_sign(0默认，9删除)
attendee_sign(0默认，1正常签到，2请假，3无卡签到，4替代签到，5迟到，9其它)

新的attendee_sign(
第一位：0默认（未签到），1正常签到，2迟到，3请假
第二位：0默认（未签到），1自己签到，2无卡签到，3替代签到
最终的标识：00默认，
		  11正常签到， 12无卡签到，13替代签到
		  21迟到，22无卡签到（迟到），23，替代签到（迟到）
		  30请假
)

会议通知表T_INFORM_LIST
INFORM_SIGN(0默认，1发送通知)

attendeeList 表中的代表团名称改成编号