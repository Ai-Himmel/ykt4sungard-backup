create table T_TIF_LUNCHRCVDTL
(
  TX_DATE       CHAR(8) not null, --交易日期
  TX_TIME       CHAR(6) not null,  --交易时间
  CARD_NO       NUMBER not null,   --卡号
  DEVICE_ID     CHAR(8) not null,
  SERIAL_NO     NUMBER not null,
  PURSE_NO      NUMBER,
  IN_BALA       NUMBER,
  OUT_BALA      NUMBER,
  AMOUNT        NUMBER,
  TOTAL_CNT     NUMBER,
  TX_CODE       NUMBER not null,
  TX_MARK       NUMBER,
  COMU_VER      NUMBER,
  RUN_REASON    NUMBER,
  CRC           CHAR(4),
  INPOWER_NO    CHAR(8),
  SYS_ID        NUMBER,
  OPER_NO       CHAR(8),
  STATUS        CHAR(1),
  COL_TIMESTAMP CHAR(20),
  FEE_CODE      NUMBER,
  ERR_CODE      NUMBER,
  MANAGE_FEE    NUMBER default 0 not null,
  OPERATE_DATE  VARCHAR2(8),
  OPERATE_TIME  VARCHAR2(6)
);

create table t_cif_cosumer_LUNCH
(
cut_id  NUMBER,        		--客户号
cut_type  NUMBER,     		--客户类别
cut_state  NUMBER,     		--客户状态
cut_name    VARCHAR2(60),       --客户姓名
stuemp_no  VARCHAR2(20),        --学工号
sex       CHAR(1),              --性别
man_id    VARCHAR2(20),         --身份证
card_id   NUMBER,               --卡号
state     NUMBER,               --状态  1：增加2：已经处理3：删除该名单
phy_id      varchar2(9)         --物理卡号
);

create table T_TIF_LUNCHRDATE
(
  ORDID         NUMBER,        	 --写1就可
  BG_DATE       CHAR(8) not null,--开始日期
  BG_TIME       CHAR(6) not null,--开始吃饭时间
  ED_DATE       CHAR(8) not null,--结束日期
  ED_TIME       CHAR(6) not null,--结束吃饭时间
  RECODE        VARCHAR2(100)    --备注
);
