create table ykt_cur.T_TIF_REPORT_STADIUM
(
  BALANCE_DATE     VARCHAR(10) not null,
  FEE_NUM          INTEGER,
  AMT              double,
  FREE_NUM         INTEGER,
  VIP_NUM          INTEGER,
  CARD_NUM         INTEGER
);

  
create table ykt_cur.T_STATIUM_FEEMNG
(
  FEE_TYPE INTEGER,
  MONEY  double,
  OPERATOR   VARCHAR(8),
  RESERVE   VARCHAR(255)
);

create table ykt_cur.T_STATIUM_VIPMNG
(
  stuemp_no char(30),
  TIMS  INTEGER,
  OPERATOR   VARCHAR(8),
  RESERVE   VARCHAR(255)
);


create table ykt_cur.T_STATIUM_FEEDATE
(
  DATA_ID     INTEGER not null,
  FEE_DATA    VARCHAR(8) not null,
  STAGE_VALUE VARCHAR(200) not null,
  OPERATOR   VARCHAR(8),
  RESERVE     VARCHAR(40)
);

  
create table ykt_cur.T_STATIUM_STAGE
(
  STAGE_ID   INTEGER,
  BEGIN_TIME VARCHAR(6),
  END_TIME   VARCHAR(6),
  OPERATOR   VARCHAR(8),
  RESERVE    VARCHAR(40)
);

create table YKT_CUR.T_STATIUM_RCVDTL
(
  DEVICE_ID  CHAR(8) not null,
  CARD_NO       integer not null,
  SERIAL_NO     integer not null,
  VIP_FLAG      CHAR(1),
  AMOUNT        double,
  STATUS        CHAR(1),
  in_DATE     CHAR(8),
  in_TIME     CHAR(6),
  OUT_DATE      CHAR(8),
  OUT_TIME      CHAR(6)
);  
create table YKT_HIS.T_STATIUM_RCVDTL_HIS
(
  Bak_DATE      CHAR(8) not null,
  BAK_TIME      CHAR(6) not null,
  DEVICE_ID  CHAR(8) not null,
  CARD_NO       integer not null,
  SERIAL_NO     integer not null,
  VIP_FLAG      CHAR(1),
  AMOUNT        double,
  STATUS        CHAR(1),
  in_DATE     CHAR(8),
  in_TIME     CHAR(6),
  OUT_DATE      CHAR(8),
  OUT_TIME      CHAR(6)
);


