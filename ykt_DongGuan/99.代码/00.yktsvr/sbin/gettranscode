set -x
ERRFILE=$HOME/ykt/src/include/transcode.h

echo "delete from ykt_cur.t_transcode;"
awk  ' $1~/#define/&&/\/\//  {n=index($0,$4);msg=substr($0,n);print $3" "msg}' $ERRFILE |sed 's#//##g'|sed 's///g'|awk '{if(NF==2){printf("insert into ykt_cur.t_transcode(transcode,transname) values(%d,%c%s%c);\n",$1,39,$2,39)}}'

