<?xml version="1.0"?>

<project name="installer-build"  basedir="${basedir}" default="install">
	<property environment="env" />

	<property file="${basedir}/ant.install.properties" />
	<property file="${basedir}/installer-build.properties" />

	<target name="install">
		<antcall target="unzip" />
		<antcall target="replace-directory-references" />
	</target>

	<target name="unzip">
		<unzip src="${app.server.pramati.zip}" dest="${app.server.pramati.install.dir}" />
	</target>

	<target name="replace-directory-references">
		<pathconvert
			property="app.server.pramati.java.home.dir.converted"
			targetos="windows"
		>
			<path>
				<pathelement location="${app.server.java.home.dir}" />
			</path>
		</pathconvert>

		<pathconvert
			property="app.server.pramati.dir.converted"
			targetos="windows"
		>
			<path>
				<pathelement location="${app.server.pramati.dir}" />
			</path>
		</pathconvert>

		<replace
			dir="${app.server.pramati.install.dir}"
			includes="**/*.bat"
		>
			<replacefilter
				token="${app.server.pramati.java.home}"
				value="${env.JAVA_HOME}"
			/>
			<replacefilter
				token="${app.server.pramati.java.home.dir.converted}"
				value="${env.JAVA_HOME}"
			/>
			<replacefilter
				token="${app.server.pramati.dir}"
				value="${app.server.pramati.install.dir}"
			/>
			<replacefilter
				token="${app.server.pramati.dir.converted}"
				value="${app.server.pramati.install.dir}"
			/>
		</replace>
	</target>
</project>