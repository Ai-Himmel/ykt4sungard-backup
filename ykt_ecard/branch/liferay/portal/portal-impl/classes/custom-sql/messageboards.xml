<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.countByS_G_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBCategory.categoryId) AS COUNT_VALUE
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.findByS_G_U">
		<![CDATA[
			SELECT
				DISTINCT {MBCategory.*}
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBCategory.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByCategoryIds">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				[$CATEGORY_ID$]
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByGroupId">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBMessage
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_U">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBMessage
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBMessage.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByGroupId">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByNoAssets">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			WHERE
				(messageId NOT IN(
					SELECT
						classPK
					FROM
						TagsAsset
					WHERE
						classNameId = [$CLASS_NAME_ID_COM.LIFERAY.PORTLET.MESSAGEBOARDS.MODEL.MBMESSAGE$]
				))
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByUuid_G">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBMessage.uuid_ = ?) AND
				(MBCategory.groupId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_U">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBMessage.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByC_C">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			INNER JOIN
				MBThread ON
					(MBThread.threadId = MBMessage.threadId)
			INNER JOIN
				MBDiscussion ON
					(MBDiscussion.threadId = MBThread.threadId)
			WHERE
				(MBDiscussion.classNameId = ?) AND
				(MBDiscussion.classPK = ?)
			ORDER BY
				MBMessage.createDate ASC,
				MBMessage.messageId ASC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFlagFinder.countByU_T">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBMessageFlag
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBMessageFlag.messageId)
			WHERE
				(MBMessageFlag.userId = ?) AND
				(MBMessage.threadId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByCategoryIds">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBThread
			WHERE
				[$CATEGORY_ID$]
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByGroupId">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByG_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBMessage.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByS_G_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByGroupId">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByG_U">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBMessage.userId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByS_G_U">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.threadId = MBThread.threadId)
			INNER JOIN
				MBCategory ON
					(MBCategory.categoryId = MBMessage.categoryId)
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
</custom-sql>