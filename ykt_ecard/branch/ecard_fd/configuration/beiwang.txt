ldap
修改了 com.liferay.portal.util.PortalUtil.java文件中的
public static User getUser(HttpServletRequest req)｛

｝
用户功能  request中没有usrid 则从 域cookie中查找用户
找到的话就把

						ses.setAttribute("j_username", userId);
						ses.setAttribute("j_password", user.getPassword());

						ses.setAttribute(WebKeys.USER_PASSWORD, user.getPassword());
						ses.setAttribute(WebKeys.USER_ID, userId);
这信息设置到session中去

